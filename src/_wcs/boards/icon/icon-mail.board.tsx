import { createBoard } from '@wixc3/react-board';
import { Icon } from '../../../components/shared/icon/icon';

export default createBoard({
    name: 'Icon - Mail',
    Board: () => <Icon name="mail" />
});
